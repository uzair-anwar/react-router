import React from "react";
import { useLocation } from "react-router-dom";

function About() {
  //let location = useLocation();
  return <p>This is about page and your are</p>;
}

export default About;
